[[permission]]
identifier = "credential-vault"
description = "Read, replace, or clear DarkBear's two fixed OS credential-vault records."
commands.allow = [
  "credential_vault_get",
  "credential_vault_set",
  "credential_vault_delete",
]
